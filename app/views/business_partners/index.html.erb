<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Business Partners</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 20px;
    }

    h1 {
      color: #333;
      text-align: center;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th, td {
      border: 1px solid #ddd;
      padding: 12px;
      text-align: left;
    }

    th {
      background-color: #007bff;
      color: #fff;
    }

    tbody tr:hover {
      background-color: #f5f5f5;
    }

    a.button {
      display: inline-block;
      padding: 8px 12px;
      text-align: center;
      text-decoration: none;
      background-color: #007bff;
      color: #fff;
      border-radius: 4px;
      transition: background-color 0.3s ease;
    }

    a.button:hover {
      background-color: #0056b3;
    }

    .new-business-partner-link {
      display: block;
      margin-top: 20px;
    }
  </style>
</head>
<body>

<h1>Business Partners</h1>

<table>
  <thead>
    <tr>
      <th>Customer Name</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @vendor_master.business_partners.each do |business_partner| %>
      <tr>
        <td><%= business_partner.customer_name %></td>
        <td>
          <%= link_to 'Show', vendor_master_business_partner_path(@vendor_master, business_partner), class: 'button' %>
          <%= link_to 'Edit', edit_vendor_master_business_partner_path(@vendor_master, business_partner), class: 'button' %>
          <%= link_to 'Delete', vendor_master_business_partner_path(@vendor_master, business_partner), method: :delete, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'button' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Business Partner', new_vendor_master_business_partner_path(@vendor_master), class: 'button new-business-partner-link' %>

</body>
</html>
